# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.wrangler

# Cache local do npm — não é código do projeto.
.npm-cache

# Backup local do projeto — não faz sentido versionar um zip do próprio repo.
FaciliteERP.zip

# Ícones brutos de teste/staging — depois de importados (ver
# scripts/import-module-icons.mjs), o que entra no app fica em
# src/assets/icons/modules; a pasta original não precisa ser versionada.
Recursos de Desenvolvimento/

# Segredos reais (o .env.example fica versionado como modelo).
.env
.env.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Cache local do CLI da Supabase — não é código do projeto.
supabase/.temp/
