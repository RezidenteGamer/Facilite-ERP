/**
 * Placeholder até o schema existir no Supabase.
 *
 * Quando o projeto for criado, gere o tipo real com:
 *   npx supabase gen types typescript --project-id <id> > src/types/supabase.ts
 *
 * Isso substitui este arquivo por tipos de tabelas/views/funções reais,
 * e src/lib/supabaseClient.ts passa a ter autocompletar de queries.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export type Database = Record<string, never>;
